let initialState = {
    
};

const cmsReducer = (state = initialState, action) => {
    switch (action.type) {
        // get 
        
        // default
        default: {
            return state
        }
    }
};

export default cmsReducer;